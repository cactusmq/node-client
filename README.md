# node-client
Example Node client for CactusMQ

## Install
$ npm i cactusmq --save

## Start a subscriber:
$ curl -N http://api.cactusmq.com/v1/subscribe/{YOUR_SUBSCRIBER_KEY}

## Run node publisher:
$ node ./index.js
